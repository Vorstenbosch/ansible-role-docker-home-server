---
# handlers file for docker_home_server

- name: restart docker
  ansible.builtin.systemd:
    name: docker
    state: restarted

- name: restart sshd
  ansible.builtin.systemd:
    name: sshd
    state: restarted
