# ansible-role-docker-home-server
This role sets up a rocky linux machine up for a home-server based on an docker compose environment.

## Features
- Docker ready to be used
- Docker compose environment running node exporter